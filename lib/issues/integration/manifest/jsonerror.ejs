<div class="col-md-12 issue">
    <div id="issue-status" class="col-md-1"><span class="octicon octicon-issue-opened"></span></div>
    <div class="col-md-11 content-issue">
    <h2 class="title-issue page-header">App manifest is not well-formed JSON <br><small><%=category%></small></h2>
        <p>The declared app manifest at <code><a href="<%= manifest %>"><%= manifest %></a></code> cannot be parsed as JSON — it fails  with the error <code><%= error.message %></code>. Browsers will not be able to use it to install the app.</p>
        <br>
        <div class='fixit'>
            <h2><span class="octicon octicon-issue-closed"></span> HOW TO FIX IT:</h2>
            <p>Make sure that the manifest file is well-formed JSON; watch out in particular that JSON only accepts double-quotes (<code>"</code>), that these are required around property names, and JSON does not accept trailing commas.</p>
        </div>
    </div>
</div>