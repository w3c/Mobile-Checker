<div class="col-md-12 issue">
    <div class="col-md-12 content-issue">
    <h2 class="title-issue page-header"><span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span> Invalid value for <%=property%> in meta viewport <small><%=category%></small></h2>
        <p>The property <code><%=property%></code> in the meta viewport declaration has an invalid value: <code><%=value%></code>.</p>
        <br>
        <p>Because <code><%=value%></code> is not a recognized value for <code><%=property%></code>, it will be ignored by the browser.</p>
        <p class='fixit'><strong>Fix it:</strong> Use a valid value for <code><%=property%></code>, one of: <%=validValues%>. Learn more about <a href="https://docs.webplatform.org/wiki/tutorials/mobile_viewport">the mobile viewport</a></p>
    </div>
</div>