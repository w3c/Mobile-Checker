<div class="col-md-12 issue">
    <div class="col-md-12 content-issue">
    <h2 class="title-issue page-header">Inconsistent usage of prefixed CSS properties. <small><%=category%></small></h2>
        <p>Some of the CSS style sheets use prefixed properties, i.e. properties that may have not been standardized yet and that are specific to a given browser.</p>
        <p>While using prefixed properties make it possible for a page to use leading edge advances in CSS,  it is important that they be used in a way that does not affect compatibility with other browsers. Some prefixed properties have inconsistent values across prefixed names.</p>
        <a data-toggle="collapse" href="#mismatchingprefixeslist">List of inconsistent prefixed properties</a>
        <table id="mismatchingprefixeslist" class="panel-collapse collapse table">
        <thead><tr><th>Location</th><th>Mismatched declarations</th></tr></thead>
        <tbody>
        <% var urls = Object.keys(mismatchingPrefixes); %>
        <% for(var i=0; i < urls.length; i++) {%>
        <tr><th colspan="2" scope='row'><a href='<%= urls[i] %>'><code><%=: urls[i] | ellipsizeUrl %></code></a></th></tr>
        <% for(var j=0; j < mismatchingPrefixes[urls[i]].length; j++) {%>
        <% var mismatchingPrefix = mismatchingPrefixes[urls[i]][j]; %>
        <tr>
        <td>line <%= mismatchingPrefix.position.line %>, col <%= mismatchingPrefix.position.column %><span class="info btn btn-xs btn-default glyphicon glyphicon-info-sign" data-container="body" title="CSS Rule" data-toggle="popover" data-placement="bottom" data-html="true" data-content="<pre><code><%= mismatchingPrefix.rule %></code></pre>"></span></td>
        <td><% for (var k=0 ; k < mismatchingPrefix.decls.length ; k++) { %>
           <code><%= mismatchingPrefix.decls[k] %></code><br>
        <%}%></td>
        </tr>
        <% } %>
        <% } %>
        </tbody>
        </table>
        <script>$('span.info').popover();</script>
        <br>
        <p>Broad compatibility with mobile browsers ensure as many users as possible will benefit from the web page. It also keeps the Web open!</p>
        <p class='fixit'><strong>Fix it:</strong> For each identified CSS rule, ensure the various prefix equivalents have the same declaration.</p>
    </div>
</div>
