//check width of document (DOM)
exports.name = "fonts-size";
exports.category = "responsive";
exports.check = function (checker) {
	var sink = checker.sink;
	for (index in checker.webAppData.fontSize){
		
	}
}