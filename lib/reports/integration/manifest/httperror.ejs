<div class="issue">
    <h2>App manifest could not be retrieved</h2>
        <p>The declared app manifest at <code><a href="<%= manifest %>"><%= manifest %></a></code> could not be retrieved — it failed with HTTP error <code><%= httperror %></code>.</p>
</div>
<div class='fix'>
            <p>Check that the address for the manifest <code><%= manifest %> is correct, and make sure it has been properly uploaded to the server.</p>
        </div>
